// app.js

let name = prompt("Enter your name:");

if (name) {
    alert("Welcome, " + name + "!");
    console.log("Welcome, " + name + "!");
} else {
    alert("Please enter your name.");
}